name: 🚀 배포

on:
  pull_request:
    types: [ closed ]
    branches: [ "main" ]

jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: JDK 17 세팅
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: gradlew 실행 권한 설정
        run: chmod +x gradlew

      - name: Gradle 캐시 설정
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: .env 생성
        env:
          DOTENV: ${{ secrets.DOTENV }}
        run: |
          printf '%s\n' "$DOTENV" > .env

      - name: application-secret.yml 생성
        run: |
          mkdir -p ./src/main/resources/yaml
          touch ./src/main/resources/yaml/application-secret.yml
          echo "spring:" >> ./src/main/resources/yaml/application-secret.yml
          echo "  config:" >> ./src/main/resources/yaml/application-secret.yml
          echo "    import: optional:file:.env[.properties]" >> ./src/main/resources/yaml/application-secret.yml
          echo "    activate:" >> ./src/main/resources/yaml/application-secret.yml
          echo "      on-profile: secret" >> ./src/main/resources/yaml/application-secret.yml
          echo "  mail:" >> ./src/main/resources/yaml/application-secret.yml
          echo "    host: smtp.naver.com" >> ./src/main/resources/yaml/application-secret.yml
          echo "    port: 465" >> ./src/main/resources/yaml/application-secret.yml
          echo "    username: ${{ secrets.MAIL_USERNAME }}" >> ./src/main/resources/yaml/application-secret.yml
          echo "    password: ${{ secrets.MAIL_PASSWORD }}" >> ./src/main/resources/yaml/application-secret.yml
          echo "    properties:" >> ./src/main/resources/yaml/application-secret.yml
          echo "      mail:" >> ./src/main/resources/yaml/application-secret.yml
          echo "        smtp:" >> ./src/main/resources/yaml/application-secret.yml
          echo "          auth: true" >> ./src/main/resources/yaml/application-secret.yml
          echo "          starttls:" >> ./src/main/resources/yaml/application-secret.yml
          echo "            enable: true" >> ./src/main/resources/yaml/application-secret.yml
          echo "            required: true" >> ./src/main/resources/yaml/application-secret.yml
          echo "          connectiontimeout: 5000" >> ./src/main/resources/yaml/application-secret.yml
          echo "          timeout: 5000" >> ./src/main/resources/yaml/application-secret.yml
          echo "          writetimeout: 5000" >> ./src/main/resources/yaml/application-secret.yml
          echo "" >> ./src/main/resources/yaml/application-secret.yml
          echo "jwt:" >> ./src/main/resources/yaml/application-secret.yml
          echo "  secret:" >> ./src/main/resources/yaml/application-secret.yml
          echo "    key: ${{ secrets.JWT_SECRET_KEY }}" >> ./src/main/resources/yaml/application-secret.yml
          echo "" >> ./src/main/resources/yaml/application-secret.yml
          echo "cloud:" >> ./src/main/resources/yaml/application-secret.yml
          echo "  aws:" >> ./src/main/resources/yaml/application-secret.yml
          echo "    s3:" >> ./src/main/resources/yaml/application-secret.yml
          echo "      bucket: usw-circle-link-storage-kr" >> ./src/main/resources/yaml/application-secret.yml
          echo "    stack:" >> ./src/main/resources/yaml/application-secret.yml
          echo "      auto: false" >> ./src/main/resources/yaml/application-secret.yml
          echo "    region:" >> ./src/main/resources/yaml/application-secret.yml
          echo "      static: ap-northeast-2" >> ./src/main/resources/yaml/application-secret.yml
          echo "    credentials:" >> ./src/main/resources/yaml/application-secret.yml
          echo "      access-key: ${{ secrets.AWS_ACCESS_KEY }}" >> ./src/main/resources/yaml/application-secret.yml
          echo "      secret-key: ${{ secrets.AWS_SECRET_KEY }}" >> ./src/main/resources/yaml/application-secret.yml

      - name: Gradle 빌드
        run: ./gradlew build --parallel

      - name: 📦 jar, appspec, 배포 스크립트 복사
        run: |
          mkdir -p before-deploy/scripts
          cp ./build/libs/*.jar ./before-deploy/
          cp ./.env ./before-deploy/
          cp -r ./scripts/. ./before-deploy/scripts/

      - name: 🗜️ 배포 패키지 생성
        run: |
          cd before-deploy
          zip -r -qq ../deploy.zip .

      - name: 🔑 SSH 키 설정
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.LIGHTSAIL_SSH_KEY }}

      - name: 🔑 Lightsail 호스트 추가
        run: |
          ssh-keyscan -H ${{ secrets.LIGHTSAIL_HOST }} >> ~/.ssh/known_hosts

      - name: 🚀 LightSail 서버에 파일 업로드
        run: |
          scp ./deploy.zip ec2-user@${{ secrets.LIGHTSAIL_HOST }}:/home/ec2-user/deploy.zip
          ssh ec2-user@${{ secrets.LIGHTSAIL_HOST }} << 'EOSSH'
            set -euo pipefail
            mkdir -p /home/ec2-user/app
            unzip -o /home/ec2-user/deploy.zip -d /home/ec2-user/app
            chmod +x /home/ec2-user/app/scripts/*.sh
          EOSSH

      - name: LightSail 서버에서 배포 실행 (AfterInstall)
        run: |
          ssh ec2-user@${{ secrets.LIGHTSAIL_HOST }} "/home/ec2-user/app/scripts/run_new_was.sh"

      - name: LightSail 서버에서 배포 실행 (ApplicationStart & Self-Heal)
        run: |
          ssh ec2-user@${{ secrets.LIGHTSAIL_HOST }} << 'EOSSH'
            set -euo pipefail
            # Ensure nginx upstream config exists for blue/green
            if [ ! -f /etc/nginx/conf.d/service-url.inc ]; then
              echo "set \$service_url http://127.0.0.1:8082;" | sudo tee /etc/nginx/conf.d/service-url.inc
              sudo service nginx reload || true
            fi
            # Initial health check; if fails, try cold restart automatically
            if ! /home/ec2-user/app/scripts/health.sh; then
              echo "[WARN] Health check failed. Attempting self-heal restart..."
              # Kill any stale WAS on both candidate ports
              for PORT in 8081 8082; do
                PID=$(lsof -Fp -i TCP:${PORT} | grep -Po 'p[0-9]+' | grep -Po '[0-9]+') || true
                if [ -n "$PID" ]; then sudo kill "$PID" || true; fi
              done
              # Force a cold-start baseline for nginx mapping
              echo "set \$service_url http://127.0.0.1:8082;" | sudo tee /etc/nginx/conf.d/service-url.inc
              sudo service nginx reload || true
              # Start new WAS and re-check health
              /home/ec2-user/app/scripts/run_new_was.sh
              /home/ec2-user/app/scripts/health.sh
            fi
          EOSSH

      - name: LightSail 서버에서 배포 실행 (ValidateService)
        run: |
          ssh ec2-user@${{ secrets.LIGHTSAIL_HOST }} "/home/ec2-user/app/scripts/switch.sh"

      - name: 임시 파일 정리
        run: |
          ssh ec2-user@${{ secrets.LIGHTSAIL_HOST }} "rm -f ~/deploy.zip"